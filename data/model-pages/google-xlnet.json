{
  "id": "google-xlnet",
  "name": "XLNet",
  "provider": "Google",
  "hf_repo": "xlnet/xlnet-large-cased",
  "hf_url": "https://huggingface.co/xlnet/xlnet-large-cased/blob/main/config.json",
  "config": {
    "architectures": [
      "XLNetLMHeadModel"
    ],
    "attn_type": "bi",
    "bi_data": false,
    "bos_token_id": 1,
    "clamp_len": -1,
    "d_head": 64,
    "d_inner": 4096,
    "d_model": 1024,
    "dropout": 0.1,
    "end_n_top": 5,
    "eos_token_id": 2,
    "ff_activation": "gelu",
    "initializer_range": 0.02,
    "layer_norm_eps": 1e-12,
    "mem_len": null,
    "model_type": "xlnet",
    "n_head": 16,
    "n_layer": 24,
    "pad_token_id": 5,
    "reuse_len": null,
    "same_length": false,
    "start_n_top": 5,
    "summary_activation": "tanh",
    "summary_last_dropout": 0.1,
    "summary_type": "last",
    "summary_use_proj": true,
    "task_specific_params": {
      "text-generation": {
        "do_sample": true,
        "max_length": 250
      }
    },
    "untie_r": true,
    "vocab_size": 32000
  },
  "description": "Here is how to read the XLNet config.json. The architecture class is XLNetLMHeadModel, which is the transformers (or equivalent) module that wires embeddings, attention, the MLP, and the language-modelling head that turns hidden states into next-token logits.\n\nhidden_size is 1,024: that is the width of every residual stream vector. The embedding matrix is therefore 32,000 \u00d7 1,024: each token id owns a vector of that width.\n\nnum_attention_heads is 16, so each head sees 64 dimensions (1,024 / 16). The ratio test is hidden_size / num_attention_heads = 1024 / 16 = 64. 64 is a multiple of 32 (NVIDIA warp-friendly), and a multiple of 16 (Huawei cube-friendly), and a power of two (Triton / FlashAttention-friendly). From this number alone you cannot name the training chip.\n\nnum_hidden_layers is 24: the stack is that many transformer blocks deep.\n\nvocab_size is 32,000. 32,000 is divisible by 256, the usual vocab-tiling grain. bos/eos ids, when present, are just two special rows in that table; the padded vocab is often larger than the tokenizer\u2019s real inventory.\n\nThe copy of config.json is from https://huggingface.co/xlnet/xlnet-large-cased/blob/main/config.json."
}
